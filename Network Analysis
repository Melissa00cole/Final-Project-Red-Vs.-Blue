Overview:

Working as a Security Engineer for X-CORP, supporting the SOC infrastructure. The SOC analysts have noticed some discrepancies with alerting in the Kibana system and the manager has asked the Security Engineering team to investigate. your team confirmed that newly created alerts are working. You will see analysis of the monitored live traffic on the wire that detected any abnormalities that aren't reflected in the alerting system.
Reported you will see all the findings for both the SOC manager and the Engineering Manager with appropriate analysis.
See the Network Analysis Report Template Below: 

At least two users on the network have been wasting time on YouTube. 
These people have created their own web server on the corporate network. 
So far, Security knows the following about these time thieves:

They have set up an Active Directory network.
They are constantly watching videos on YouTube.
Their IP addresses are somewhere in the range 10.6.12.0/24.

After inspecting the traffic we know the following:

the domain name of the users' custom site is:
frank-n-ted.com

the IP address of the Domain Controller (DC) of the AD network is: 
10.6.12.12

The name of the malware downloaded to the 10.6.12.203 machine is:
june11.dll

The malware is classified as:
Trojan
 
 
 
Vulnerable Windows Machines

The Security team received reports of an infected Windows host on the network. They know the following:
Machines in the network live in the range 172.16.4.0/24.
The domain mind-hammer.net is associated with the infected computer.
The DC for this network lives at 172.16.4.4 and is named Mind-Hammer-DC.
The network has standard gateway and broadcast addresses.


the following information is about the infected Windows machine:
Host name:	ROTTERDAM-PC
IP address:	172.16.4.205
MAC address:	00:59:07:b0:63:a4

username of the Windows user whose computer is infected:
mattijs.dervies

 the IP address used in the actual infection traffic are:
185.243.115.84


Illegal Downloads

IT was informed that some users are torrenting on the network. The Security team does not forbid the use of torrents for legitimate purposes, such as downloading operating systems.
However, they have a strict policy against copyright infringement.
IT shared the following about the torrent activity:

The machines using torrents live in the range 10.0.0.0/24 and are clients of an AD domain.

The DC of this domain lives at 10.0.0.2 and is named DogOfTheYear-DC.

The DC is associated with the domain dogoftheyear.net.

After isolating the torrent traffic we know the following information about the machine with IP address 10.0.0.201:

MAC address:		00:16:17:18:66:c8

Windows username:		elmer.blanco

OS version:		BLANCO-DESKTOP

The torrent file the user downloaded was:   Betty_Boop_Rhythm_on_the_Reservation.avi.torrent
